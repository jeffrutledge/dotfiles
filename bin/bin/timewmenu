#!/bin/zsh
# 
# Opens demnu to starts a task in timew

choice=$(

for i in {1..$(timew get dom.tag.count)}
do
  timew get dom.tag.$i
done | eval 'dmenu "$@"'

)

if [ $choice ]; then
  timew start $choice
fi
